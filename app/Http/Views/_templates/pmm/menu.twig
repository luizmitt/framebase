<div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
    </button>

</div>
<div class="navbar-collapse collapse">

    <!-- MENU ESQUERDA -->
    <ul class="nav navbar-nav">
        <li>
            <a href="{{URL}}painel">
                <span class="glyphicon glyphicon-home" aria-hidden="true"></span>
                INÍCIO
            </a>
        </li>
        {{generateMenu(configure['app']['APP_NAME']) | raw}} 
    </ul>

    <!-- MENU DIREITA -->
    <ul class="nav navbar-nav navbar-right" style="margin-right:5px">
        <li>
            <a href="#" data-toggle="dropdown">
                <span class="glyphicon glyphicon-cog" aria-hidden="true"></span>
                <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
                <li>
                    <a class="dropdown-header" style='font-size:0.80em; font-weight:bold'>{{session.TX_LOGIN}}</a>
                    <a style='font-size:0.65em'>{{environment_current}}</a>
                </li>
                <li role="separator" class="divider"></li>
                <li>
                    <a class="dropdown-header">Ferramentas</a>
                </li>
                {% if session.s_environment == "dev" %}
                    <li>
                        <a href="{{URL}}docs/">
                            <span class="glyphicon glyphicon-book" aria-hidden="true"></span>
                            Documentação</a>
                    </li>
                    <li>
                        <a href="{{URL}}pmm-generator">
                            <span class="glyphicon glyphicon-wrench" aria-hidden="true"></span>
                            Gerador de Código</a>
                    </li>
                {% endif %}

                <li>
                    <a href='#' onclick="javascript:carregaLogGit();" data-toggle="modal" data-target="#gitLog">
                        <i class='glyphicon glyphicon-zoom-in'></i>
                        Histórico de Atualizações</a>
                </li>

                <li>
                    <a href='#' data-toggle="modal" data-target="#alterarSenhaPmm">
                        <i class='fa fa-recycle'></i>
                        Alterar sua senha</a>
                </li>

                {% if session.s_environment == "pro" %}
                    <li>
                        <a class="dropdown-header">Meus Sistemas</a>
                        {#getUserSys() |raw#}
                    </li>
                {% endif %}
                <li class="dropdown-header">Sair do Sistema</li>
                <li>
                    <a href="{{URL}}autenticacao/logout">
                        <span class="glyphicon glyphicon-off" aria-hidden="true"></span>
                        Sair</a>
                </li>
                <li>
                    <a href="#" style="font-size:small">
                        {{thupan_version}}
                    </a>
                </li>
            </ul>
        </li>
    </ul>
</div>
